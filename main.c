#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "libft.h"
#include "ft_printf.h"

int main()
{
	//ft_printf("this %i number", 17);
	ft_printf("=%i\n", ft_printf (tst, w, p, c));
    printf("=%i\n",    printf (tst, w, p, c));
	return (0);
}